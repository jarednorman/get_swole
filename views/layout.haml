!!! 5
%html
  %head
    %meta{ charset: "UTF-8" }
    %meta(name="viewport" content="width=device-width, initial-scale=1")
    %link{ rel: 'stylesheet', type: 'text/css', href: asset_url("swole.css") }
    %link{ rel: 'stylesheet', type: 'text/css', href: '//fonts.googleapis.com/css?family=Karla:300,400,400italic,700' }
    %title Get Swole
  %body
    = partial("layout/header")

    = yield

    = partial("layout/footer")

    %script{ src: asset_url("swole.js") }

    = partial("layout/tracking")
